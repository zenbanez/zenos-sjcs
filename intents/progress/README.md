# Intent Progress

Progress registers are linked read models. Their source intent, workspace,
delegation, decision, and warrant records prevail.

Allowed operational states: `not-started`, `in-progress`,
`awaiting-participant`, `awaiting-integration`, `awaiting-steward`, `blocked`,
`deferred`, `completed`, and `reconciliation-needed`.
