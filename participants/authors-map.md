# Git Author Map

No local author is registered yet.

After steward commissioning, add exact Git author strings:

| Git author string | Participant ID |
|---|---|
| `Example Name <example@zenos.local>` | `example-participant` |

Delete the example row when adding the first real mapping. If one author string
maps to multiple participants, every relevant commit must carry a
`Zen-Participant: <id>` trailer.
