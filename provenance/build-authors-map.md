# RC0 Build Author Map

This file maps distribution-build commits only. It is provenance, not a local
instance participant registry.

| Git author string | Build identity |
|---|---|
| `Sol <sol@zenos.local>` | `rc0-integration-builder` |
| `Zen Bañez <zenbanez@yahoo.com>` | `rc0-commissioning-review-steward` |

The steward row covers the GitHub-generated PR merge candidate and any later
steward-approved RC0 review merge. It records who carries the candidate across
that boundary; it grants no participant standing in a receiving instance.
